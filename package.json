{
  "name": "personal-dashboard",
  "version": "1.0.0",
  "description": "Personal dashboard single-page app",
  "scripts": {
    "test": "vitest --run"
  },
  "devDependencies": {
    "fast-check": "^3.22.0",
    "vitest": "^2.1.0"
  }
}
